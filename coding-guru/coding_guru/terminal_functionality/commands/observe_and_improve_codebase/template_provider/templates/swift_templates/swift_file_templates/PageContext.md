import SwiftUI

@Observable
final class <<<PageName>>>PageContext {
    // MARK: - Properties
    // Dependencies
    private(set) var pageOperations: <<<PageName>>>PageOperations!

    // Constants

    // Core Properties

    // Presentation Properties

    // Navigation Properties

    // Computed Properties

    // MARK: - Initializers
    init() {
        self.pageOperations = <<<PageName>>>PageOperations(pageContext: self)
    }
} 